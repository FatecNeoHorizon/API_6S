{
  "name": "zeus",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "prepare": "git config core.hooksPath .githooks"
  },
  "dependencies": {
    "turbo": "latest"
  }
}
